# BMM Workflow Status
# This tracks progress through BMM methodology phases.
# Implementation phase is tracked separately in sprint-status.yaml

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met
#   - skipped: Optional workflow that was not selected
#   - included: Optional workflow selected by user
#
# Completion Status:
#   - {file-path}: File created/found (replaces initial status)

generated: "2025-12-13"
project: "rss-remastered"
project_type: "software"
selected_track: "bmad-method"
field_type: "greenfield"
workflow_path: "method-greenfield.yaml"

workflow_status:
  phase_0_discovery:
    name: "Discovery (Optional)"
    workflows:
      brainstorm-project:
        status: "docs/analysis/brainstorming-session-2025-12-13.md"
        agent: "analyst"
        command: "/bmad:bmm:agents:analyst then *brainstorm-project"
      research:
        status: "skipped"
        agent: "analyst"
        command: "/bmad:bmm:workflows:research"
      product-brief:
        status: "skipped"
        agent: "analyst"
        command: "/bmad:bmm:workflows:create-product-brief"

  phase_1_planning:
    name: "Planning"
    workflows:
      prd:
        status: "docs/prd.md"
        agent: "pm"
        command: "/bmad:bmm:workflows:create-prd"
        output: "Product Requirements Document"
        completed: "2025-12-13"
      create-ux-design:
        status: "docs/ux-design-specification.md"
        agent: "ux-designer"
        command: "/bmad:bmm:workflows:create-ux-design"
        completed: "2025-12-13"

  phase_2_solutioning:
    name: "Solutioning"
    workflows:
      create-architecture:
        status: "docs/architecture.md"
        agent: "architect"
        command: "/bmad:bmm:workflows:create-architecture"
        output: "System architecture document"
        completed: "2025-12-13"
      generate-project-context:
        status: "docs/project_context.md"
        agent: "architect"
        command: "/bmad:bmm:workflows:generate-project-context"
        output: "AI agent implementation guide"
        completed: "2025-12-13"
      create-epics-and-stories:
        status: "docs/epics.md"
        agent: "pm"
        command: "/bmad:bmm:workflows:create-epics-stories"
        output: "7 epics, 70 stories with acceptance criteria"
        completed: "2025-12-13"
      test-design:
        status: "recommended"
        agent: "tea"
        command: "/bmad:bmm:workflows:testarch-test-design"
        output: "System-level testability review"
      validate-architecture:
        status: "skipped"
        agent: "architect"
        command: "/bmad:bmm:agents:architect then validate"
        note: "Validation included in create-architecture workflow"
      implementation-readiness:
        status: "required"
        agent: "architect"
        command: "/bmad:bmm:workflows:check-implementation-readiness"
        note: "Gate check before implementation"

  phase_3_implementation:
    name: "Implementation"
    workflows:
      sprint-planning:
        status: "required"
        agent: "sm"
        command: "/bmad:bmm:workflows:sprint-planning"
        note: "Creates sprint plan - subsequent work tracked in sprint-status.yaml"
